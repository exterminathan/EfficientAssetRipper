version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    # Update each pinned `uses: foo/bar@<sha>` line to the latest release SHA
    # without touching the version comment unless we accept the bump.
    open-pull-requests-limit: 5
    labels:
      - dependencies
      - github-actions
