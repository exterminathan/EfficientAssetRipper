# Test + build dependencies (not needed at runtime).
# Install with: py -m pip install -r requirements-dev.txt
#
# Versions are pinned to upper bounds so a future major release can't silently
# break the test gate or the PyInstaller-generated bundle.  When tightening
# the pin further, prefer regenerating with `pip-compile --generate-hashes`.

pytest>=8.3,<9
pytest-qt>=4.4,<5
pytest-mock>=3.14,<4
pytest-cov>=5.0,<6
pytest-xdist>=3.6,<4
coverage[toml]>=7.6,<8

# Build-time tooling.  Lives here (not in requirements.txt) so end-user
# installs of the Python sources stay slim.
pyinstaller>=6.0,<7
