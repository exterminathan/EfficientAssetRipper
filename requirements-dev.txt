# Test suite dependencies (not needed at runtime).
# Install with: py -m pip install -r requirements-dev.txt

pytest>=8.3,<9
pytest-qt>=4.4,<5
pytest-mock>=3.14,<4
pytest-cov>=5.0,<6
pytest-xdist>=3.6,<4
coverage[toml]>=7.6,<8
